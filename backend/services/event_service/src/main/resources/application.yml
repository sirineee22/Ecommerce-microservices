server:
  port: 8083

spring:
  application:
    name: EVENT-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/event_service_db
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

