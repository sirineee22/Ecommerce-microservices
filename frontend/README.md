# Frontend - MyShop (Angular)

Angular e-commerce marketplace frontend.

## Setup

```bash
npm install
npm start
```

Runs at http://localhost:4200

## API Configuration

Update `src/environments/` to point to the API Gateway at `http://localhost:8080` when using the microservices backend.
